# Installazione nodo zdos

Installazione automatica:

```
curl -sL http://<ip>/install-zdos-node.sh | bash
```

Config mainnet:

```
{
  "network": "mainnet",
  "seed": "http://<ip>:8091/nodes.json",
  "bootstrap_rpc": "http://<ip>:8181"
}
```
